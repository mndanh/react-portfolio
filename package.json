{
  "name": "react-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "cd client && npx vite",
    "install": "cd clientr && npm i",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "save": "^2.9.0",
    "validator": "^13.12.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
